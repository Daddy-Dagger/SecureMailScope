# Model artifacts

Place explicitly approved local model artifacts here. Binary model files are ignored by Git by default and should include provenance and reproducibility notes when intentionally distributed.

