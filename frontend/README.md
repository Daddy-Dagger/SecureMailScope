# Frontend

This is a minimal React/Vite development client. It currently displays project identity and checks the backend `GET /health` endpoint. It is not the final dashboard.

```bash
npm ci
npm run dev
```
