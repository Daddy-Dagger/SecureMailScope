# Scripts

- `setup.sh` — creates a Unix/macOS virtual environment and installs Python and frontend dependencies
- `setup.ps1` — equivalent Windows PowerShell setup
- `check_tshark.py` — validates TShark availability and prints its version
- `generate_test_data.py` — reserved placeholder; no capture generator exists yet

